## CREATE CHROMA DB DATABASE (RUN THIS ONLY ONCE)

```
python build_database.py
```

And currently, we are changing the user request in the `user_request.txt` file. 

```
python main.py -u "user_request.txt"
```